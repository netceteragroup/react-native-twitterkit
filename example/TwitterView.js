// Video.js
import { requireNativeComponent } from 'react-native';
// requireNativeComponent automatically resolves this to “TwitterViewManager”
module.exports = requireNativeComponent('TwitterView', null);
